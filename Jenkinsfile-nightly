pipeline {
    agent any

    stages {
        stage('Build Docker Images') {
            steps {
                sh 'docker build -f ci/Dockerfile-playright -t my-playwright --network=host .'
                sh 'docker build -f ci/Dockerfile-aws-cli -t my-aws-cli --network=host .'
            }
        }
    }
}